"use client";

import { useState } from "react";

export function AskForFix() {
  const [request, setRequest] = useState(""); const [sent, setSent] = useState(false);
  const suggestions = ["I rebuild the same report every week.", "I copy the same information between apps.", "I repeat the same checks before work goes out."];
  const submitRequest = () => { if (request.trim()) setSent(true); };
  return <div className="mx-auto max-w-[960px] pt-[7px]"><header className="max-w-[720px]"><h1 className="text-balance text-[34px] font-normal leading-[1.2] tracking-[-0.035em] text-black">Ask Dystil to look closer.</h1><p className="mt-[16px] text-[18px] leading-[1.65] text-[#596069]">Point to one part of your work that feels repetitive, slow, or more manual than it should be. Dystil will watch for that pattern first.</p></header><form className="mt-[38px] max-w-[860px]" onSubmit={(event) => { event.preventDefault(); submitRequest(); }}><label htmlFor="fix-request" className="block text-[15px] font-medium text-[#252826]">What should Dystil pay attention to?</label><div className="mt-[12px] overflow-hidden rounded-[12px] border border-[#d8d7d2] bg-white transition-colors focus-within:border-[#6ba893] focus-within:ring-1 focus-within:ring-[#6ba893]"><div className="px-[18px] py-[15px]"><textarea id="fix-request" value={request} rows={3} maxLength={600} onChange={(event) => { setRequest(event.target.value); setSent(false); }} onKeyDown={(event) => { if (event.key === "Enter" && (event.metaKey || event.ctrlKey)) { event.preventDefault(); submitRequest(); } }} placeholder="Describe the task, where it happens, and what makes it annoying…" className="block min-h-[92px] max-h-[180px] w-full resize-y bg-transparent text-[18px] leading-[1.55] text-black outline-none placeholder:text-[#8b9095]" /></div><div className="flex min-h-[62px] items-center justify-between gap-5 border-t border-[#eceae5] bg-[#fbfbfa] px-[15px] py-[9px]"><div className="flex min-w-0 items-center gap-[9px] text-[14px] text-[#737980]"><span className="h-[7px] w-[7px] shrink-0 rounded-full bg-[#12a77a]" aria-hidden="true" /><span>Stays on this computer</span><span className="text-[#a1a5aa]">·</span><span>{request.length}/600</span></div><button type="submit" disabled={!request.trim()} className="h-[42px] shrink-0 rounded-[9px] bg-[#087b5f] px-[20px] text-[16px] font-medium text-white transition-colors hover:bg-[#06634d] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#087b5f] disabled:cursor-not-allowed disabled:bg-[#d8dfdb] disabled:text-[#8a928e]">Look into this</button></div></div><p className={`mt-[14px] min-h-[26px] text-[15px] ${sent ? "text-[#087b5f]" : "text-[#83888e]"}`} aria-live="polite">{sent ? "Got it. Dystil will look for this pattern first." : "Press ⌘ Enter to submit"}</p></form><section className="mt-[28px] max-w-[720px]"><p className="text-[14px] font-medium text-[#737980]">Not sure how to phrase it?</p><div className="mt-[9px] divide-y divide-[#e7e5df] border-y border-[#e7e5df]">{suggestions.map((suggestion) => <button key={suggestion} type="button" onClick={() => { setRequest(suggestion); setSent(false); }} className="group flex min-h-[48px] w-full items-center justify-between gap-5 text-left text-[16px] text-[#4f555c] transition-colors hover:text-[#087b5f] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#087b5f]"><span>{suggestion}</span><span className="text-[#a5aaa7] transition-transform group-hover:translate-x-1 group-hover:text-[#087b5f]" aria-hidden="true">→</span></button>)}</div></section></div>;
}
